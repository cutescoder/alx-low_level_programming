#include "main.h"

/**
 * _abs - A function that computes value of an integer
 * @n: main character
 * Return: Always 0 (Success)
 */

int _abs(int n)
{

	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
